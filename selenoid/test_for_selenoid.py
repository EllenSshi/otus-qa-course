def test_for_selenoid(remote_browser):
    remote_browser.get("https://aerokube.com/cm/latest/")
    assert remote_browser.title == "Configuration Manager"

def test_for_selenoid_2(remote_browser):
    remote_browser.get("https://aerokube.com/cm/latest/")
    assert remote_browser.title == "Configuration Manager"

def test_for_selenoid_3(remote_browser):
    remote_browser.get("https://aerokube.com/cm/latest/")
    assert remote_browser.title == "Configuration Manager"

def test_for_selenoid_4(remote_browser):
    remote_browser.get("https://aerokube.com/cm/latest/")
    assert remote_browser.title == "Configuration Manager"
