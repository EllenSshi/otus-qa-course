def test_app(remote_browser, base_url):
    remote_browser.get(base_url + "/opencart/")
    assert remote_browser.title == "Your Store"


def test_app2(remote_browser, base_url):
    remote_browser.get(base_url + "/opencart/")
    assert remote_browser.title == "Your Store"


def test_app3(remote_browser, base_url):
    remote_browser.get(base_url + "/opencart/")
    assert remote_browser.title == "Your Store"


def test_app4(remote_browser, base_url):
    remote_browser.get(base_url + "/opencart/")
    assert remote_browser.title == "Your Store"


def test_app5(remote_browser, base_url):
    remote_browser.get(base_url + "/opencart/")
    assert remote_browser.title == "Your Store"


def test_app6(remote_browser, base_url):
    remote_browser.get(base_url + "/opencart/")
    assert remote_browser.title == "Your Store"
